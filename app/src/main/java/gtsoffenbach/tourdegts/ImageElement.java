package gtsoffenbach.tourdegts;

/**
 * Created by Noli on 31.08.2014.
 */
public class ImageElement extends UIElement {

    ImageElement(final ElementContainer container, final int dx, final int dy, final int sx, final int sy) {
        super(container, dx, dy, sx, sy);
    }

    public void update(float delta) {
        super.update(delta);
    }
}
